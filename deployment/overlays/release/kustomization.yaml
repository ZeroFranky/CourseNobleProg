apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
patches: 
- path: configmap-patch.yaml
  target:
    kind: ConfigMap
    name: coursenobleprog
- path: deployment-w-storage-patch.yaml
  target:
    kind: Deployment
    name: coursenobleprog
- path: service-patch.yaml
  target:
    kind: Service
    name: coursenobleprog

resources: 
- ../../base

nameSuffix: -dev
